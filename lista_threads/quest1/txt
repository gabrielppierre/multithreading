#READ-ME

- Abra um terminal e navegue até a pasta onde os arquivos estão salvos.
- Compile os arquivos api.c e main.c usando um compilador C, como o GCC, utilizando o seguinte comando:
    gcc -o main q1.c main.c -lpthread

-O parâmetro -lpthread é necessário para incluir a biblioteca de threads pthread na compilação.
-Este comando irá compilar os arquivos api.c e main.c e gerar um executável chamado main.
-Execute o arquivo gerado com o seguinte comando no terminal:
    ./main
